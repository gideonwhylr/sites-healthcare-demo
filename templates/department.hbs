---
{
    "stream": {
        "$id": "departments",
        "source": "knowledgeGraph",
        "destination": "pages",
        "fields": [
            "id", 
            "uid", 
            "name", 
            "address", 
            "mainPhone",
            "description", 
            "photoGallery",
            "slug"
        ], 
        "filter" : {
            "entityTypes": [
                "ce_department"
            ]
        }
    },
    "urlFormat": "{{slug}}"
}
---
<!DOCTYPE html>
<html lang="en">
    <head>
        {{>head}}
        <title>{{name}}</title>
        {{>schema}}
        <link rel="shortcut icon" type="image/jpg" href="/images/yext-favicon.png"/>
    </head>
    <body>      
        <div class="w-full">
            {{>nav}}
            <div class="centered-container grid gap-y-5">
                <div class="section">{{>department-core-information}}</div>
                <div class="section">{{>department-relatedStories}}</div>
            </div>
            <div class="section">{{>department-whatNext}}</div>
            <div class="w-full bg-ach-blue">
                <div class="centered-container">
                    {{>footer}}
                </div>
            </div>
        </div>
    </body> 
</html>